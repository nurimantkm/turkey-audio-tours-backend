# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/turkey_audio_tours

# JWT Secret (Generate a strong secret key)
JWT_SECRET=your-super-secret-jwt-key-here

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# File Upload Configuration (if using file uploads)
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# Email Configuration (for future features)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# External API Keys (for future integrations)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Map Services (if needed)
MAPBOX_ACCESS_TOKEN=your_mapbox_token_here

# Redis Configuration (for caching, if needed)
REDIS_URL=redis://localhost:6379

